# SteadyCut → Gumroad launch pack

Persisted in the repo (`launch/`). Decisions locked: commercial, **$19 one-time + support**, **wedding/event videographers**, **stable-window finder / Premiere-supported** (DaVinci + AI features experimental). Repo is private.

## Read in this order
1. **`LAUNCH-CHECKLIST.md`** — the master list: what's blocking, what I need from you (demo video, account, decisions), what I produce. **Start here.**
2. `gumroad-listing.md` — product copy to paste (title, description, requirements, receipt, refund).
3. `gumroad-runbook.md` — the ~20-min click-by-click publish steps.
4. `marketing-posts.md` — launch posts + the 20-sec demo-video script.

## Still to generate (mine)
- `cover.png` + gallery screenshots — re-shot against the **new UI** once the build is final (the old ones in scratchpad are the previous design).

## The one thing that matters most
The **demo video on real footage**. Folder-in → colour-coded Premiere timeline out. Record it before you post anywhere.

## Open blockers (see checklist)
- Test the new build on real footage.
- Confirm Windows XML now points at original clips, not the `proxies/` subfolder.
- Cut a fresh CI build after fixes land.
